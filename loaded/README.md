# Loaded — static site

Self-contained static export of loaded.framer.website (all JS, fonts, images and CMS
chunks mirrored locally under `js/`, `cdn/`, `images/`, `fonts/`). Framer badge and the
Framer analytics/editor-bar script removed.

Run locally:

    python3 -m http.server 8082
